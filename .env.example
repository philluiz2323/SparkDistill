# Teacher provider credentials — copy to .env and fill in, never commit .env itself.

# Anthropic (Claude Fable 5 — `claude-fable-5`)
ANTHROPIC_API_KEY=

# OpenAI (GPT 5.6 — `gpt-5.6`)
OPENAI_API_KEY=
